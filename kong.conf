#!/bin/bash
#
# Kong 测试脚本统一配置
# 所有 test/*.sh 脚本共享此配置
# 环境变量优先级: 环境变量 > 本文件默认值
#

# Kong 网关地址
KONG_HOST="${KONG_HOST:-8.165.50.42}"
KONG_PORT="${KONG_PORT:-80}"
KONG_URL="http://${KONG_HOST}:${KONG_PORT}"

# 认证 Token
AUTH_TOKEN="${AUTH_TOKEN:-eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJvbGUiOiJzZXJ2aWNlX3JvbGUiLCJpYXQiOjE3NzU3MDQxMzcsImV4cCI6MTMyODYzNDQxMzd9.0Z6nDWW6QSLnlONtyrb2z-BIUxX6iEmjxyzNmDZi8XE}"

# 模型名称
MODEL="${MODEL:-qwen3.6-plus}"

# 压测通用参数
MAX_TOKENS="${MAX_TOKENS:-1000}"
TEMPERATURE="${TEMPERATURE:-0.7}"

# 测试数据目录
DATA_DIR="${DATA_DIR:-/tmp/llm_benchmark_data}"

# 结果输出目录
RESULT_DIR="${RESULT_DIR:-/tmp/llm_benchmark_results}"

# 多 Key 轮转配置 (可选)
RDS_PG_API_KEY_LIST="${RDS_PG_API_KEY_LIST:-sk-31ede6823ff64a0fb44044c69b8c6d1e}"
RDS_PG_API_KEY_STRATEGY="${RDS_PG_API_KEY_STRATEGY:-round-robin}"
